import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    flexDirection: 'column'
  },
  strong: {
    margin: 15
  },
  footer: {
    marginTop: 75 // TODO: For some reason the button is floating
  }
})
